{
  "name": "hardbox-proxy",
  "version": "0.0.2",
  "description": "Proxy Module for hardbox",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "test-cov": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha ./test/*.js",
    "coveralls": "yarn test-cov -- --report lcovonly && cat ./coverage/lcov.info | coveralls"
  },
  "keywords": [
    "hardbox",
    "proxy"
  ],
  "author": "Tomasz Rakowski",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "coveralls": "^3.0.4",
    "eslint": "^5.16.0",
    "istanbul": "^0.4.5",
    "mocha": "^6.1.4",
    "mocha-lcov-reporter": "^1.3.0",
    "request": "^2.88.0"
  },
  "dependencies": {
    "global": "^4.4.0",
    "hardbox-proxy": "^0.0.1",
    "http-proxy": "^1.17.0"
  }
}
